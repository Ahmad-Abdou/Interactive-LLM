# Interactive-LLM
This project is focusing on emerging interactivity into large language models by leveraging interactive windows for users when prompting AI

## Environment variables

Before running the app, copy `.env.example` to `.env` and set:

- `GEMINI_API_KEY`
- `FLASK_SECRET_KEY`

`.env` is ignored by git, so secrets stay local and out of commits.
